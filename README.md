# game-nodejs

## Install NodeJS
```
$ sudo apt-get update
$ sudo apt-get install npm
$ sudo npm install -g n
$ sudo n latest

$ sudo npm install -g nodemon
```

## Run
```
#only once
$ npm install

$ nodemon app.js
```